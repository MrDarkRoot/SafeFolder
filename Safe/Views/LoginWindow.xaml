<Window x:Class="Safe.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Safe.Views"
        xmlns:vm="clr-namespace:Safe.ViewModels"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:helpers="clr-namespace:Safe.Helpers"
        mc:Ignorable="d"
        Title="Safe Folder - Login" 
        Height="450" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#1e272e"
        WindowStyle="None"
        ResizeMode="NoResize"
        MouseDown="Window_MouseDown">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2980b9"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3498db"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Close Button -->
            <Button Grid.Row="0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Width="30"
                    Height="30"
                    Padding="0"
                    Background="Transparent"
                    Click="CloseButton_Click">
                <iconPacks:PackIconMaterial Kind="Close" 
                                          Width="20" 
                                          Height="20"/>
            </Button>

            <!-- Main Content -->
            <StackPanel Grid.Row="1" 
                        VerticalAlignment="Center">
                <!-- Logo/App Title -->
                <StackPanel HorizontalAlignment="Center" 
                           Margin="0,0,0,30">
                    <iconPacks:PackIconMaterial Kind="FolderLock" 
                                              Width="64" 
                                              Height="64" 
                                              Foreground="#2980b9"/>
                    <TextBlock Text="Safe Folder"
                             FontSize="24"
                             FontWeight="Bold"
                             HorizontalAlignment="Center"
                             Margin="0,10"/>
                </StackPanel>

                <!-- Password Input -->
                <PasswordBox x:Name="passwordBox"
                           helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                           Background="#2c3e50"
                           Foreground="White"
                           BorderThickness="0"
                           Padding="10"
                           Height="40"
                           Margin="0,0,0,20"/>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}"
                         Foreground="{Binding IsError, Converter={StaticResource BoolToColorConverter}}"
                         TextAlignment="Center"
                         TextWrapping="Wrap"
                         Margin="0,0,0,20"
                         Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

                <!-- Login Button -->
                <Button Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=passwordBox, Path=Password}"
                        Content="Login"
                        Height="40"
                        FontSize="14"
                        Margin="0,0,0,10" Click="Button_Click"/>

                <!-- Setup Password Button (Only visible first time) -->
                <Button Command="{Binding SetupPasswordCommand}"
                        CommandParameter="{Binding ElementName=passwordBox, Path=Password}"
                        Content="Set Password"
                        Height="40"
                        FontSize="14"
                        Background="#27ae60"
                        Visibility="{Binding IsFirstTime, Converter={StaticResource BoolToVisibilityConverter}}" Click="Button_Click_1"/>
            </StackPanel>

            <!-- Version Info -->
            <TextBlock Grid.Row="2"
                     Text="Version 1.0"
                     FontSize="12"
                     Opacity="0.5"
                     HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</Window>