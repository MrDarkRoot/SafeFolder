<UserControl x:Class="SafeFolder.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SafeFolder.Views"
             xmlns:viewmodels="clr-namespace:SafeFolder.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="400" Background="#F0F0F0">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <!-- First Run View -->
            <StackPanel Visibility="{Binding IsFirstRun, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Chào mừng đến với Safe Folder" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Vui lòng thiết lập Mật khẩu Chính của bạn." TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Mật khẩu mới:" Margin="0,10,0,5"/>
                <PasswordBox x:Name="NewPasswordBox" Height="28"/>

                <TextBlock Text="Xác nhận mật khẩu:" Margin="0,10,0,5"/>
                <PasswordBox x:Name="ConfirmPasswordBox" Height="28"/>

                <Button Content="Thiết lập Mật khẩu" Command="{Binding SetPasswordCommand}" Height="35" Margin="0,20,0,0" Background="#007ACC" Foreground="White"/>
            </StackPanel>

            <!-- Existing User Login View -->
            <StackPanel Visibility="{Binding IsExistingUser, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Đăng nhập Safe Folder" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Mật khẩu Chính:" Margin="0,10,0,5"/>
                <PasswordBox x:Name="PasswordBox" Height="28"/>

                <Button Content="Đăng nhập" Command="{Binding LoginCommand}" IsDefault="True" Height="35" Margin="0,20,0,0" Background="#007ACC" Foreground="White"/>
            </StackPanel>

            <!-- Error Message Display -->
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,15,0,0" MinHeight="30"/>
        </StackPanel>
    </Grid>
</UserControl>
